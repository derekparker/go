Task 10 review C1 hardware verification (reviewer's exact check)
Host: numa-dell (2 nodes, 128 CPUs each, 256 total)
Command (both runs):
  GODEBUG=numa=1 GOMAXPROCS=256 <testprog binary> NUMASoftAffinity &
  PID=$!
  sleep 1.5
  for t in /proc/$PID/task/*; do grep -h Cpus_allowed_list $t/status; done | sort | uniq -c

=== PRE-FIX (SHA b1ff5da820, before numaWidenBeforeClone's newosproc call site existed) ===
    406 Cpus_allowed_list:	1,3,5,7,...,255   (node 1's 128 CPUs -- the odd-numbered ones on this host)
stdout tail: "softaffinity narrowed=406 total=406 gomaxprocs=256"

100% collapse onto ONE node (node 1), zero threads on node 0. This is
exactly why the pre-review test still reported "narrowed=406 total=406"
(every thread individually WAS narrowed to *a* single node) despite the
process being completely broken: the old probe only checked "is each
thread narrowed to a single node", never "do threads collectively span
more than one node" (review I2's fix).

=== POST-FIX (SHA b630846abf, numaWidenBeforeClone called from both
syscall_runtime_BeforeFork and newosproc) ===
    126 Cpus_allowed_list:	0,2,4,...,254     (node 0's 128 CPUs)
    136 Cpus_allowed_list:	0-255             (unnarrowed: sysmon, template thread, etc.)
    129 Cpus_allowed_list:	1,3,5,...,255     (node 1's 128 CPUs)
stdout tail: "softaffinity narrowed=273 total=393 gomaxprocs=256 nodes=0,1"

Masks from BOTH nodes present, roughly balanced (126 vs 129) -- the
review's pass criterion, met.
